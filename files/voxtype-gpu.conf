# home-os: Voxtype über die Vulkan-GPU laufen lassen (NVIDIA via nvidia_icd).
# Der /usr/bin/voxtype-Wrapper wählt bei VOXTYPE_GPU=1 das Vulkan-Binary.
# Vermeidet den read-only-Konflikt von `voxtype setup gpu` auf dem OSTree-System.
[Service]
Environment=VOXTYPE_GPU=1
