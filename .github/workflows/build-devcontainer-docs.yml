name: Build Dev Container (dev-docs)

on:
  push:
    branches: [main, master]
    paths:
      - 'devcontainer/dev-docs/**'
      - '.github/workflows/build-devcontainer-docs.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'devcontainer/dev-docs/**'
      - '.github/workflows/build-devcontainer-docs.yml'
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  build-and-scan:
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/template-build-scan.yml
    with:
      image_name: 'dev-docs'
      context: './devcontainer/dev-docs'
      dockerfile: './devcontainer/dev-docs/Containerfile'
    secrets: inherit